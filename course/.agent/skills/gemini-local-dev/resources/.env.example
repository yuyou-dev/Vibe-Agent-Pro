# .env.example

# Server Configuration
PORT=8080

# Internal Mirror Configuration
# DO NOT expose this URL to the frontend/browser
TARGET_BASE_URL=https://target-api-domain.com

# Authentication
# The secret used to sign requests. Must match the Mirror's expected secret.
AUTH_SECRET=changeme_to_your_real_secret
